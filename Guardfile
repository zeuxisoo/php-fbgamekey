# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{app/.+\.(html)})
  watch(%r{app/helpers/.+\.php})
  watch(%r{(public/).+\.(css|js|html|coffee)})
end
